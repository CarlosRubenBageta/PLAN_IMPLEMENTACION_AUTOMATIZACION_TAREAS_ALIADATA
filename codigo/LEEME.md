# codigo/ — pendiente de acción humana

**Acción requerida (Rubén):** copiar el script productivo actual desde el editor de Apps Script y guardarlo aquí como `script_actual.gs`. Guardar además una copia fechada en `respaldos/script/`.

Archivos que se generarán en fases posteriores (no crear ahora):

`appsscript.json`, `script_refactorizado.gs`, `prompts_ia.gs`, `esquema_json.gs`, `cliente_openai.gs`, `filtros_correo.gs`, `gmail_api.gs`, `idempotencia.gs`, `recuperacion.gs`, `escritura_sheets.gs`, `sanitizacion.gs`, `validaciones.gs`, `utilidades.gs`.

Regla: ningún archivo de esta carpeta debe contener credenciales.
